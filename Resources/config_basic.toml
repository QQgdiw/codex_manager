schema_version = "1.0"
profile_id = "basic"
name = "Basic software development"
description = "General coding work without a fixed hardware platform or language."
approval_policy = "approved_only"
required_capabilities = [
    "browser_retrieval",
    "resource_synthesis",
    "workspace_file_access",
    "planning",
    "code_review"
]
enabled_tools = []

[constraints]
minimal_default_per_capability = true
requires_approved_tools = true
allow_network_research = true
allow_manual_trigger_only = true

[notes]
reason_no_enabled_tools = "The current whitelist has no approved tools. Task 17 must record approval before this profile can enable tools."
