schema_version = "1.0"
profile_id = "mcu"
name = "MCU and embedded firmware"
description = "MCU, device driver, STM32, RP2350, PX4, and embedded C/C++ work."
approval_policy = "approved_only"
required_capabilities = [
    "browser_retrieval",
    "resource_synthesis",
    "workspace_file_access",
    "datasheet_lookup",
    "build_log_analysis",
    "embedded_debug_notes"
]
enabled_tools = []

[constraints]
minimal_default_per_capability = true
requires_approved_tools = true
allow_network_research = true
allow_manual_trigger_only = true

[notes]
reason_no_enabled_tools = "The current whitelist has no approved tools. Enable only approved browser, filesystem, and context tools after Task 17."
